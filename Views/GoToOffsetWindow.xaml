<Window x:Class="NexusProgrammer.GoToOffsetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Go to" Width="320" Height="130"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource AppBackgroundBrush}"
    Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource ThemedButtonStyle}" />
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="54" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Offset" VerticalAlignment="Center" />
        <TextBox x:Name="OffsetBox" Grid.Row="0" Grid.Column="1" Height="24"
            KeyDown="OffsetBox_KeyDown" />

        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
            Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="76" Height="26" IsDefault="True"
                Click="Ok_Click">Go</Button>
        </StackPanel>
    </Grid>
</Window>
