<Window x:Class="NexusProgrammer.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="About" Width="460" Height="270"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource AppBackgroundBrush}"
    Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource ThemedButtonStyle}" />
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="42" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="76" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Row="0" Grid.Column="0" Width="56" Height="56"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="0,22,0,0" Source="/Assets/icon.ico" />

            <StackPanel Grid.Row="0" Grid.Column="1">
                <TextBlock x:Name="TitleText" FontSize="22" FontWeight="SemiBold" />
                <TextBlock TextWrapping="Wrap" Margin="0,10,0,0"
                    Text="A modern BIOS programming tool for hardware technicians and repair professionals" />
                <TextBlock TextWrapping="Wrap" Margin="0,12,0,0">
                    <Run Text="Supports " />
                    <Run Text="CH341 &#183; CH347 &#183; XGecu T48 &#183; RT809F &#183; RT809H"
                        FontWeight="SemiBold" />
                </TextBlock>
            </StackPanel>

            <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,18,0,16" />

            <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Text="Developed by mhqb365 &#169; 2026" FontWeight="SemiBold" />
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
            VerticalAlignment="Top" Margin="0,4,0,0">
            <Button Width="110" Height="28" Margin="0,0,8,0" Click="SourceCode_Click">Source Code</Button>
            <Button Width="86" Height="28" Click="Author_Click">Author</Button>
        </StackPanel>
    </Grid>
</Window>
